seasons = ("spring", "summer", "autumn", "winter")

month = input("Please enter the month from 1 to 12 (e.g.: for January, enter '1'.):\n")

try:
    int(month)
    month = int(month)
    if month >= 1 and month <= 12:
        if int()
    else:
        print()